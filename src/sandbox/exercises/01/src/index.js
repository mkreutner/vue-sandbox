/**
 * Soustrait le deuxième nombre (b) du premier (a).
 * @param {number} a - Le premier nombre (le diminuende).
 * @param {number} b - Le deuxième nombre (le diminuteur).
 * @returns {number} Le résultat de a - b.
 */
export const substract = (a, b) => {
  return a - b;
};
